export const DECODE_FAULT_PHASES = Object.freeze([
  Object.freeze({ codes: Object.freeze(['DECODE_TYPE'] as const) }),
  Object.freeze({ codes: Object.freeze(['DECODE_LIMIT'] as const) }),
  Object.freeze({ codes: Object.freeze(['DECODE_SCHEMA'] as const) }),
  Object.freeze({ codes: Object.freeze(['DECODE_REFERENCE'] as const) }),
  Object.freeze({ codes: Object.freeze(['DECODE_GRAPH'] as const) }),
  Object.freeze({ codes: Object.freeze(['DECODE_CANONICAL'] as const) }),
] as const);
