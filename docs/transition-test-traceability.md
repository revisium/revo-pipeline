# Transition Test Traceability

This matrix maps the Accepted transition contract and the current testing matrices to executable
named tests or the Accepted graph-invariant proof where a state is structurally unreachable. It
characterizes shipped behavior; it does not add a decoder or reducer contract.

## Transition contract rows

| Normative row                                         | Named test evidence                                                                                                                                                                                                                                                                                                                     |
| ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Node × state: task, branch, terminal                  | `core pipeline transitions > characterizes entry-omitted, enabled, and terminal fact states for task, branch, and terminal nodes`; `core pipeline transitions > non-entry omitted nodes emit no decision before their activation edge is satisfied`                                                                                     |
| Node × state: fork, join                              | `fork and join coordination > characterizes entry-omitted fork and pre-activation omitted join states plus enabled and terminal facts`                                                                                                                                                                                                  |
| Node × state: consensus                               | `consensus coordination > characterizes omitted, enabled, and terminal fact states for consensus nodes`                                                                                                                                                                                                                                 |
| Node × state: human gate                              | `human gate coordination > characterizes omitted, enabled, and terminal fact states for human-gate nodes`                                                                                                                                                                                                                               |
| Non-entry omissions across all seven kinds            | `all-seven-node pure scenario > keeps non-entry omissions inert and progresses without hidden coordination state`; the entry branch is enabled while task, fork, join, consensus, human-gate, and terminal facts are omitted.                                                                                                           |
| Empty facts, entry activation, repeated intent        | `core pipeline transitions > activates entry and replays the same deterministic decision`                                                                                                                                                                                                                                               |
| Post-application continuation                         | `core pipeline transitions > waits for an enabled task and activates its missing successor after completion`                                                                                                                                                                                                                            |
| Post-application continuation with a present target   | `core pipeline transitions > continues after an applied branch target is present and returns its wait`                                                                                                                                                                                                                                  |
| Empty selector activation                             | The Accepted `Quiescence and empty-activation invariant` proves this defensive branch unreachable for compiler-produced graphs and valid facts; fabricating such a fact snapshot would violate causal closure.                                                                                                                          |
| Quiescent noop                                        | The same Accepted invariant gives the exhaustive frontier proof. `preserves exact accepted scalar, outcome, node and decision unions` locks the retained public `noop` discriminator without claiming it is emitted for a valid snapshot.                                                                                               |
| One/two terminals and terminal precedence             | `core pipeline transitions > terminal precedence is stable for enabled and terminal terminal-node facts`; `core pipeline transitions > faults precede an otherwise reached terminal and multiple terminals are rejected`; `fork and join coordination > a reached terminal outranks earlier actionable residual work after an any join` |
| Faults, action-before-wait, causal and atomic targets | `core pipeline transitions > rejects malformed, foreign, duplicate, mistyped, and noncausal facts before progress`; `core pipeline transitions > actions outrank waits in canonical topological order`; `fork and join coordination > terminal fork requires every entry and join atomically`                                           |
| Branch partition                                      | `core pipeline transitions > branch waits for its declared fact, then selects exact or default outcome`; `core pipeline transitions > terminal branch outcome must agree with the retained value selection`                                                                                                                             |
| Join partition                                        | `fork and join coordination > covers every two-branch status partition for join policy $kind`                                                                                                                                                                                                                                           |
| Consensus partition                                   | `consensus coordination > selects consensus policy %o outcome %s`; `consensus coordination > waits for incomplete consensus policy %o`                                                                                                                                                                                                  |
| Gate partition                                        | `human gate coordination > waits unresolved and selects every declared resolution`; `human gate coordination > rejects invalid, duplicate, foreign and premature resolutions`                                                                                                                                                           |
| Compiled round-trip and tamper                        | `core pipeline transitions > validates JSON-round-tripped compiled data and rejects integrity drift`; `compiled integrity > rejects incomplete node indexes and ghost edge targets`                                                                                                                                                     |
| Bounds, descriptors, ordering, truncation             | `fact bounds and diagnostics > prechecks every fact-array bound and aggregate before traversal`; `fact bounds and diagnostics > returns the globally first 99 faults and root truncation sentinel`                                                                                                                                      |
| Determinism, permutation, isolation                   | `core pipeline transitions > activates entry and replays the same deterministic decision`; `core pipeline transitions > is invariant to valid fact permutation`; `all-seven-node pure scenario > keeps non-entry omissions inert and progresses without hidden coordination state`                                                      |

## `docs/testing.md` rows

| Matrix row                                   | Named test evidence                                                                                                                                                                                                                                                                                                                                         |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Slice 1: contracts, root/package, DAG probes | `exposes every accepted contract through curated type-only barrels`; `ships exactly the accepted explicit source and runtime root manifest`; `accepts the complete layer dependency matrix`; `rejects $1.path with $2`                                                                                                                                      |
| Slice 2: contracts, policy, limits, faults   | `preserves exact accepted scalar, outcome, node and decision unions`; `copies, normalizes and recursively freezes dense JSON data`; `publishes the exact deeply frozen v1 limits`; `uses exact cross-class decision phases`                                                                                                                                 |
| Slice 3: compiler and graph                  | `builds canonical frozen portable graph data and exact indexes`; `derives forward, reverse and barrier-bounded reachability`; `rejects incomplete node indexes and ghost edge targets`                                                                                                                                                                      |
| Slice 4: core transition                     | `core pipeline transitions > characterizes entry-omitted, enabled, and terminal fact states for task, branch, and terminal nodes`; `core pipeline transitions > terminal precedence is stable for enabled and terminal terminal-node facts`                                                                                                                 |
| Slice 5a: fork/join                          | `fork and join coordination > covers every two-branch status partition for join policy $kind`                                                                                                                                                                                                                                                               |
| Slice 5b: consensus/human gate               | `consensus coordination > selects consensus policy %o outcome %s`; `human gate coordination > waits unresolved and selects every declared resolution`                                                                                                                                                                                                       |
| Slice 6: packaging and consumer proof        | `ships exactly the accepted explicit source and runtime root manifest`; `supports the documented deterministic public root workflow with isolated compiled data`; the one-tarball portion is the named `verify:package` gate, because it needs its built tarball rather than a Vitest fixture.                                                              |
| Node state                                   | The state-characterization and all-seven omission tests above distinguish entry omission from inert non-entry omission for all seven kinds.                                                                                                                                                                                                                 |
| Progression                                  | `core pipeline transitions > activates entry and replays the same deterministic decision`; `core pipeline transitions > waits for an enabled task and activates its missing successor after completion`; `core pipeline transitions > continues after an applied branch target is present and returns its wait`; Accepted quiescence/empty-activation proof |
| Terminal                                     | `core pipeline transitions > terminal precedence is stable for enabled and terminal terminal-node facts`; `core pipeline transitions > faults precede an otherwise reached terminal and multiple terminals are rejected`; `fork and join coordination > a reached terminal outranks earlier actionable residual work after an any join`                     |
| Causality                                    | `fork and join coordination > terminal fork requires every entry and join atomically`; `human gate coordination > terminal gate replays its declared resolution and requires the routed target`                                                                                                                                                             |
| Branch                                       | `core pipeline transitions > branch waits for its declared fact, then selects exact or default outcome`; `rejects retained branch integrity drift: %s`                                                                                                                                                                                                      |
| Join                                         | `fork and join coordination > covers every two-branch status partition for join policy $kind`                                                                                                                                                                                                                                                               |
| Consensus                                    | `consensus coordination > selects consensus policy %o outcome %s`                                                                                                                                                                                                                                                                                           |
| Gate                                         | `human gate coordination > waits unresolved and selects every declared resolution`                                                                                                                                                                                                                                                                          |
| Compiled data                                | `compiled integrity > rejects noncanonical scalar normalization instead of repairing compiled input`; `compiled integrity > rejects a canonical forged graph whose fork branches overlap`                                                                                                                                                                   |
| Bounded input                                | `fact bounds and diagnostics > prechecks every fact-array bound and aggregate before traversal`; `compiled integrity > prunes oversized nested case and predicate arrays before element inspection`                                                                                                                                                         |
| Reflection                                   | `compiled integrity > rejects in-range accessor elements without invoking node, case, or edge-offset getters`                                                                                                                                                                                                                                               |
| Diagnostics                                  | `fact bounds and diagnostics > returns the globally first 99 faults and root truncation sentinel`; `fact bounds and diagnostics > retains independent portable sibling faults with their exact paths`                                                                                                                                                       |
| Determinism                                  | `core pipeline transitions > is invariant to valid fact permutation`; `all-seven-node pure scenario > keeps non-entry omissions inert and progresses without hidden coordination state`                                                                                                                                                                     |
